[pull]
	ff = only
[user]
	name = Cuong (Kane) Le
	email = cuong.le@inspectorio.com
	signingkey = 5008D0EFA326B408
[core]
	autocrlf = false
[commit]
	gpgsign = false
[gpg]
	program = gpg
[push]
	gpgsign = false
[log]
	showSignature = true
[url "git@github.com:"]
	insteadOf = https://github.com/
# Repos under ~/KANE/ (e.g. dotfiles) use the personal identity; everything else
# keeps the company identity above. Placed last so it overrides the default.
[includeIf "gitdir:~/KANE/"]
	path = ~/KANE/dotfiles/configs/git/gitconfig-personal
